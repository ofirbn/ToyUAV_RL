Good. Proceed with M2 only.

Goal of M2:
Add support for loading optional expert checkpoints from models/experts/, but do not yet route by mode unless an expert is explicitly present.

Requirements:
- Keep default single-model fallback behavior unchanged.
- Add models/experts/ directory support.
- Load experts by flight mode name, for example:
  models/experts/stabilize.zip
  models/experts/recovery.zip
  models/experts/alt_hold.zip
  models/experts/hdg_hold.zip
  models/experts/waypoint.zip
  models/experts/loiter.zip
  models/experts/approach.zip
  models/experts/landing.zip
- If a mode-specific expert exists, register it in MissionManager._experts.
- If it does not exist, that mode must still use the default policy.
- Do not change rewards.
- Do not change training.
- Do not change environment dynamics.
- Do not add smooth transitions yet.
- Do not implement new training scripts yet.

Before editing, list exact files you intend to modify.

After implementation, provide:
1. files changed
2. unified diffs
3. commands to test
4. expected output
5. rollback instructions